services:
  frontend:
    container_name: frontend
    image: nginx
    ports:
      - "80:80"
    environment:
      - NGINX_PORT=80
    restart: unless-stopped
    volumes:
      - "./frontend:/usr/share/nginx/html/"

